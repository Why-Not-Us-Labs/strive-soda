{%- comment -%}
Science: Electrolytes Deep Dive
- Hover cards showing benefits and amounts
- Comparison table
{%- endcomment -%}

<section class="electrolytes-deep-dive" id="electrolytes">
  <div class="science-container">
    <div class="section-intro">
      <h2 class="section-title">{{ section.settings.heading }}</h2>
      <p class="section-desc">{{ section.settings.description }}</p>
    </div>

    <div class="electrolyte-cards">
      
      <!-- Sodium Card -->
      <div class="electrolyte-deep-card sodium-card">
        <div class="card-front">
          <span class="electrolyte-badge">Essential Electrolyte</span>
          <h3 class="electrolyte-name">Sodium</h3>
          <p class="electrolyte-tagline">{{ section.settings.sodium_tagline }}</p>
          <div class="hover-hint">Hover to learn more →</div>
        </div>
        <div class="card-back">
          <h4 class="back-title">Why Sodium Matters</h4>
          <ul class="benefit-list">
            <li><strong>Fluid Balance:</strong> Regulates water retention and distribution in your body</li>
            <li><strong>Nerve Function:</strong> Essential for transmitting electrical signals</li>
            <li><strong>Muscle Contraction:</strong> Prevents cramping during activity</li>
          </ul>
          <div class="electrolyte-amount">
            <span class="amount-label">In Each Can:</span>
            <span class="amount-value">{{ section.settings.sodium_amount }}mg Sodium</span>
          </div>
        </div>
      </div>

      <!-- Potassium Card -->
      <div class="electrolyte-deep-card potassium-card">
        <div class="card-front">
          <span class="electrolyte-badge">Muscle Power</span>
          <h3 class="electrolyte-name">Potassium</h3>
          <p class="electrolyte-tagline">{{ section.settings.potassium_tagline }}</p>
          <div class="hover-hint">Hover to learn more →</div>
        </div>
        <div class="card-back">
          <h4 class="back-title">Why Potassium Matters</h4>
          <ul class="benefit-list">
            <li><strong>Heart Health:</strong> Regulates heartbeat and blood pressure</li>
            <li><strong>Muscle Recovery:</strong> Reduces soreness and fatigue</li>
            <li><strong>Cellular Function:</strong> Maintains proper cell voltage</li>
          </ul>
          <div class="electrolyte-amount">
            <span class="amount-label">In Each Can:</span>
            <span class="amount-value">{{ section.settings.potassium_amount }}mg Potassium</span>
          </div>
        </div>
      </div>

      <!-- Magnesium Card -->
      <div class="electrolyte-deep-card magnesium-card">
        <div class="card-front">
          <span class="electrolyte-badge">Energy Catalyst</span>
          <h3 class="electrolyte-name">Magnesium</h3>
          <p class="electrolyte-tagline">{{ section.settings.magnesium_tagline }}</p>
          <div class="hover-hint">Hover to learn more →</div>
        </div>
        <div class="card-back">
          <h4 class="back-title">Why Magnesium Matters</h4>
          <ul class="benefit-list">
            <li><strong>Energy Production:</strong> Converts food into usable energy</li>
            <li><strong>Muscle Relaxation:</strong> Prevents cramping and spasms</li>
            <li><strong>Bone Health:</strong> Works with calcium for strong bones</li>
          </ul>
          <div class="electrolyte-amount">
            <span class="amount-label">In Each Can:</span>
            <span class="amount-value">{{ section.settings.magnesium_amount }}mg Magnesium</span>
          </div>
        </div>
      </div>

    </div>

    {%- comment -%}
    Comparison table removed - now handled by unified "Strive vs Everyone" section
    Keep the electrolyte flip cards above - those are the 10/10 content!
    {%- endcomment -%}

  </div>
</section>

<style>

:root {
  --tiffany-blue: #0ABAB5;
  --strive-orange: #FF7A00;
  --gold-yellow: #FFD700;
  --font-primary: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
}

.science-container {
  max-width: 1400px;
  margin: 0 auto;
  padding: 0 clamp(20px, 5vw, 60px);
}

.section-intro {
  text-align: center;
  margin-bottom: clamp(40px, 6vw, 80px);
}

.section-title {
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  font-size: clamp(32px, 4vw, 52px); /* Match 75% dehydration heading template */
  font-weight: 500 !important;
  line-height: 1.05; /* Match template line-height */
  margin: 0 0 16px;
  color: #111;
}

.section-desc {
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  font-size: clamp(16px, 1.8vw, 22px);
  line-height: 1.5;
  max-width: 800px;
  margin: 0 auto;
  color: #333;
}

.electrolytes-deep-dive {
  padding: clamp(60px, 8vw, 120px) 0;
  background: #F5F1E8;
  position: relative;
  z-index: 2; /* Higher than checkerboard folds */
  isolation: isolate; /* Prevent z-index issues */
}

.electrolyte-cards {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
  gap: clamp(20px, 3vw, 40px);
  margin-bottom: clamp(60px, 8vw, 100px);
}

.electrolyte-deep-card {
  position: relative;
  aspect-ratio: 3 / 4;
  border: 3px solid #111;
  cursor: pointer;
}

.card-front,
.card-back {
  position: absolute;
  width: 100%;
  height: 100%;
  padding: clamp(24px, 4vw, 40px);
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  text-align: center;
  transition: opacity 0.4s ease, visibility 0.4s ease;
}

.card-front {
  opacity: 1;
  visibility: visible;
}

.card-back {
  opacity: 0;
  visibility: hidden;
  background: #fff;
}

.electrolyte-deep-card:hover .card-front,
.electrolyte-deep-card.flipped .card-front {
  opacity: 0;
  visibility: hidden;
}

.electrolyte-deep-card:hover .card-back,
.electrolyte-deep-card.flipped .card-back {
  opacity: 1;
  visibility: visible;
}

.sodium-card .card-front { background: var(--gold-yellow); }
.potassium-card .card-front { background: var(--tiffany-blue); }
.magnesium-card .card-front { background: var(--strive-orange); }

.electrolyte-badge {
  padding: 6px 16px;
  background: #fff;
  border: 2px solid #111;
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 1px;
  text-transform: uppercase;
  margin-bottom: 20px;
  color: #111;
}

.electrolyte-name {
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  font-size: clamp(32px, 4vw, 48px);
  font-weight: 500 !important;
  margin: 0 0 16px;
  color: #111;
}

.electrolyte-tagline {
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  font-size: clamp(14px, 1.6vw, 18px);
  line-height: 1.4;
  margin: 0 0 24px;
  color: #111;
}

.hover-hint {
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  font-size: 13px;
  font-weight: 700;
  color: #111;
  opacity: 0.7;
}

.back-title {
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  font-size: clamp(20px, 2.5vw, 28px);
  font-weight: 900;
  margin: 0 0 20px;
  color: #111;
}

.benefit-list {
  list-style: none;
  padding: 0;
  margin: 0 0 24px;
  text-align: left;
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
}

.benefit-list li {
  font-size: clamp(13px, 1.4vw, 16px);
  line-height: 1.6;
  margin-bottom: 12px;
  color: #333;
}

.benefit-list strong {
  color: #111;
  font-weight: 800;
}

.electrolyte-amount {
  padding: 12px 20px;
  background: var(--tiffany-blue);
  color: #fff;
  border-radius: 8px;
  display: flex;
  flex-direction: column;
  gap: 4px;
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
}

.amount-label {
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  opacity: 0.9;
}

.amount-value {
  font-size: 18px;
  font-weight: 900;
}

.comparison-section {
  margin-top: clamp(60px, 8vw, 100px);
}

.comparison-title {
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  font-size: clamp(28px, 3.5vw, 42px);
  font-weight: 900;
  text-align: center;
  margin: 0 0 clamp(32px, 5vw, 60px);
  color: #111;
}

.comparison-table-wrapper {
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
}

.comparison-table {
  width: 100%;
  border-collapse: collapse;
  background: #fff;
  border: 2px solid #111;
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  font-size: clamp(13px, 1.4vw, 16px);
}

.comparison-table th,
.comparison-table td {
  padding: clamp(12px, 2vw, 20px);
  text-align: left;
  border: 1px solid #ddd;
}

.comparison-table th {
  background: #111;
  color: #fff;
  font-weight: 800;
  text-transform: uppercase;
  font-size: clamp(11px, 1.2vw, 14px);
  letter-spacing: 0.5px;
}

.strive-row {
  background: rgba(10, 186, 181, 0.1);
  font-weight: 700;
}

.product-name {
  font-weight: 700;
}

/* Mobile: tap instead of hover */
@media (max-width: 768px) {
  .hover-hint::after {
    content: ' (tap)';
  }

  .electrolyte-deep-card {
    cursor: pointer;
  }

  /* Disable hover on touch devices, use tap instead */
  @media (hover: none) {
    .electrolyte-deep-card:hover .card-front {
      opacity: 1;
      visibility: visible;
    }

    .electrolyte-deep-card:hover .card-back {
      opacity: 0;
      visibility: hidden;
    }
  }
}
</style>

<script>
// Tap to flip on touch devices
document.querySelectorAll('.electrolyte-deep-card').forEach(card => {
  card.addEventListener('click', function() {
    // Only toggle on touch devices or small screens
    if (window.matchMedia('(hover: none)').matches || window.innerWidth <= 768) {
      // Close other cards first
      document.querySelectorAll('.electrolyte-deep-card.flipped').forEach(c => {
        if (c !== this) c.classList.remove('flipped');
      });
      this.classList.toggle('flipped');
    }
  });
});
</script>

{% schema %}
{
  "name": "Science: Electrolytes",
  "disabled_on": { "groups": ["header", "footer", "aside"] },
  "settings": [
    {
      "type": "header",
      "content": "Section Content"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Main heading",
      "default": "What is Hydration Soda?"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Hydration soda packs the three essential electrolytes your body needs to stay balanced, energized, and ready for anything."
    },
    {
      "type": "header",
      "content": "Sodium"
    },
    {
      "type": "text",
      "id": "sodium_tagline",
      "label": "Sodium tagline",
      "default": "Replenishes what you sweat out to keep fluids balanced."
    },
    {
      "type": "text",
      "id": "sodium_amount",
      "label": "Sodium amount (mg)",
      "default": "100"
    },
    {
      "type": "header",
      "content": "Potassium"
    },
    {
      "type": "text",
      "id": "potassium_tagline",
      "label": "Potassium tagline",
      "default": "Supports muscle and nerve function; balances sodium."
    },
    {
      "type": "text",
      "id": "potassium_amount",
      "label": "Potassium amount (mg)",
      "default": "100"
    },
    {
      "type": "header",
      "content": "Magnesium"
    },
    {
      "type": "text",
      "id": "magnesium_tagline",
      "label": "Magnesium tagline",
      "default": "Aids energy and muscle function; helps prevent cramps."
    },
    {
      "type": "text",
      "id": "magnesium_amount",
      "label": "Magnesium amount (mg)",
      "default": "100"
    },
    {
      "type": "header",
      "content": "Comparison Table"
    },
    {
      "type": "checkbox",
      "id": "show_comparison",
      "label": "Show comparison table",
      "default": true
    },
    {
      "type": "text",
      "id": "comp_soda_sodium",
      "label": "Traditional Soda - Sodium (mg)",
      "default": "50"
    },
    {
      "type": "text",
      "id": "comp_soda_potassium",
      "label": "Traditional Soda - Potassium (mg)",
      "default": "0"
    },
    {
      "type": "text",
      "id": "comp_soda_magnesium",
      "label": "Traditional Soda - Magnesium (mg)",
      "default": "0"
    },
    {
      "type": "text",
      "id": "comp_soda_sweetener",
      "label": "Traditional Soda - Sweetener",
      "default": "HFCS"
    },
    {
      "type": "text",
      "id": "comp_sports_sodium",
      "label": "Sports Drink - Sodium (mg)",
      "default": "110"
    },
    {
      "type": "text",
      "id": "comp_sports_potassium",
      "label": "Sports Drink - Potassium (mg)",
      "default": "30"
    },
    {
      "type": "text",
      "id": "comp_sports_magnesium",
      "label": "Sports Drink - Magnesium (mg)",
      "default": "0"
    },
    {
      "type": "text",
      "id": "comp_sports_sweetener",
      "label": "Sports Drink - Sweetener",
      "default": "Sugar"
    }
  ],
  "presets": [
    {
      "name": "Science: Electrolytes"
    }
  ]
}
{% endschema %}