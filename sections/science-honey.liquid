{%- comment -%}
Science: Why Honey Section
- Split layout with image
- Benefit cards
- Sweetener comparison
{%- endcomment -%}

<section class="why-honey-section" id="honey">
  <div class="science-container">
    
    <div class="honey-hero">
      <div class="honey-content">
        <h2 class="honey-title">{{ section.settings.heading }}</h2>
        <p class="honey-subtitle">{{ section.settings.subtitle }}</p>
      </div>
      <div class="honey-image">
        {% if section.settings.honey_image != blank %}
          <img src="{{ section.settings.honey_image | image_url: width: 800 }}"
               srcset="{{ section.settings.honey_image | image_url: width: 400 }} 400w,
                       {{ section.settings.honey_image | image_url: width: 800 }} 800w,
                       {{ section.settings.honey_image | image_url: width: 1200 }} 1200w"
               sizes="(min-width: 768px) 50vw, 100vw"
               alt="{{ section.settings.honey_image.alt | default: 'Organic Wildflower Honey' | escape }}"
               width="{{ section.settings.honey_image.width }}"
               height="{{ section.settings.honey_image.height }}"
               loading="lazy"
               class="honey-img">
        {% else %}
          <div class="honey-image-placeholder">
            Click "Customize" to add your honey image
          </div>
        {% endif %}
      </div>
    </div>

    <div class="honey-benefits">
      <div class="honey-benefit-card">
        <div class="benefit-icon">🍯</div>
        <h3>{{ section.settings.benefit1_title }}</h3>
        <p>{{ section.settings.benefit1_text }}</p>
      </div>
      <div class="honey-benefit-card">
        <div class="benefit-icon">💪</div>
        <h3>{{ section.settings.benefit2_title }}</h3>
        <p>{{ section.settings.benefit2_text }}</p>
      </div>
      <div class="honey-benefit-card">
        <div class="benefit-icon">🌿</div>
        <h3>{{ section.settings.benefit3_title }}</h3>
        <p>{{ section.settings.benefit3_text }}</p>
      </div>
    </div>

    {%- comment -%}
    Comparison table removed - now handled by unified "Strive vs Everyone" section
    {%- endcomment -%}

  </div>
</section>

<style>

:root {
  --tiffany-blue: #0ABAB5;
  --strive-orange: #FF7A00;
  --font-primary: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
}

.science-container {
  max-width: 1400px;
  margin: 0 auto;
  padding: 0 clamp(20px, 5vw, 60px);
}

.why-honey-section {
  padding: clamp(60px, 8vw, 120px) 0;
  background: #fff;
  scroll-margin-top: 80px;
  font-family: var(--font-primary);
}

.honey-hero {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: clamp(40px, 6vw, 80px);
  align-items: center;
  margin-bottom: clamp(60px, 8vw, 100px);
}

.honey-title {
  font-size: clamp(36px, 5vw, 64px);
  font-weight: 900;
  line-height: 1.05;
  margin: 0 0 16px;
  color: #111;
}

.honey-subtitle {
  font-size: clamp(16px, 1.8vw, 22px);
  line-height: 1.5;
  color: #333;
  margin: 0;
}

.honey-image {
  aspect-ratio: 4 / 3;
  overflow: hidden;
  border: 3px solid #111;
  background: #f5f1e8;
}

.honey-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
}

.honey-image-placeholder {
  width: 100%;
  height: 100%;
  background: linear-gradient(135deg, #f0e5d8, #e8d7c3);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 800;
  color: #666;
  font-size: 14px;
}

.honey-benefits {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: clamp(24px, 4vw, 40px);
  margin-bottom: clamp(60px, 8vw, 100px);
}

.honey-benefit-card {
  padding: clamp(28px, 4vw, 40px);
  background: #F5F1E8;
  border: 2px solid #111;
  text-align: center;
}

.benefit-icon {
  font-size: 48px;
  margin-bottom: 16px;
}

.honey-benefit-card h3 {
  font-size: clamp(20px, 2.5vw, 28px);
  font-weight: 900;
  margin: 0 0 12px;
  color: #111;
}

.honey-benefit-card p {
  font-size: clamp(14px, 1.5vw, 17px);
  line-height: 1.5;
  margin: 0;
  color: #333;
}

.honey-comparison {
  margin-top: clamp(60px, 8vw, 100px);
}

.comparison-title {
  font-size: clamp(28px, 3.5vw, 42px);
  font-weight: 900;
  text-align: center;
  margin: 0 0 clamp(32px, 5vw, 60px);
  color: #111;
}

.sweetener-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  gap: clamp(20px, 3vw, 32px);
}

.sweetener-card {
  padding: clamp(24px, 4vw, 36px);
  background: #fff;
  border: 3px solid #ddd;
}

.sweetener-card.winner {
  border-color: var(--tiffany-blue);
  background: rgba(10, 186, 181, 0.05);
}

.sweetener-card h4 {
  font-size: clamp(18px, 2vw, 24px);
  font-weight: 900;
  margin: 0 0 16px;
  color: #111;
}

.sweetener-card ul {
  list-style: none;
  padding: 0;
  margin: 0;
}

.sweetener-card li {
  font-size: clamp(13px, 1.4vw, 16px);
  line-height: 1.6;
  margin-bottom: 8px;
  color: #333;
}

@media (max-width: 768px) {
  .honey-hero {
    grid-template-columns: 1fr;
  }
}
</style>

{% schema %}
{
  "name": "Science: Why Honey",
  "disabled_on": { "groups": ["header", "footer", "aside"] },
  "settings": [
    {
      "type": "header",
      "content": "Hero Content"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Main heading",
      "default": "Why Honey?"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "100% sweetened with organic wildflower honey — nature's perfect fuel."
    },
    {
      "type": "image_picker",
      "id": "honey_image",
      "label": "Honey image"
    },
    {
      "type": "header",
      "content": "Benefits"
    },
    {
      "type": "text",
      "id": "benefit1_title",
      "label": "Benefit 1 Title",
      "default": "Natural Energy"
    },
    {
      "type": "textarea",
      "id": "benefit1_text",
      "label": "Benefit 1 Text",
      "default": "Honey provides quick, sustained energy from natural sugars without the crash of refined sugar."
    },
    {
      "type": "text",
      "id": "benefit2_title",
      "label": "Benefit 2 Title",
      "default": "Healthy Carbs"
    },
    {
      "type": "textarea",
      "id": "benefit2_text",
      "label": "Benefit 2 Text",
      "default": "Unlike high-fructose corn syrup, honey contains trace minerals and antioxidants that support your body."
    },
    {
      "type": "text",
      "id": "benefit3_title",
      "label": "Benefit 3 Title",
      "default": "Pure & Organic"
    },
    {
      "type": "textarea",
      "id": "benefit3_text",
      "label": "Benefit 3 Text",
      "default": "Our organic wildflower honey is unprocessed, unfiltered, and sourced from sustainable beekeepers."
    },
    {
      "type": "header",
      "content": "Comparison"
    },
    {
      "type": "checkbox",
      "id": "show_comparison",
      "label": "Show sweetener comparison",
      "default": true
    }
  ],
  "presets": [
    {
      "name": "Science: Why Honey"
    }
  ]
}
{% endschema %}